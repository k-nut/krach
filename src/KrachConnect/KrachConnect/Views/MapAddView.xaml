<UserControl x:Class="KrachConnect.Views.MapAddView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition></ColumnDefinition>
      <ColumnDefinition></ColumnDefinition>
    </Grid.ColumnDefinitions>
    <StackPanel Grid.Column="0">
      <TextBlock FontSize="20" Text="{Binding SelectedMeasuringPoint.Model.Name}" />
      <TextBlock FontSize="20" Text="Messwerte hinzufügen" />
      <TextBlock Text="Datum" />
      <DatePicker DataContext="{Binding NewNoiseMeasurement.MeasurementDate}" />
      <Label Content="Uhrzeit" />
      <TextBox Text="12:13" />
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition />
          <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          <ColumnDefinition />
          <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Messverfahren" Grid.Row="0" Grid.Column="0" />
        <ComboBox Grid.Row="1" Grid.Column="0" SelectedIndex="1">
          <ComboBoxItem Content="DIN 12121" />
          <ComboBoxItem Content="ISO 3141" />
          <ComboBoxItem Content="VDI 12178" />
        </ComboBox>

        <TextBlock Grid.Row="0" Grid.Column="1" Text="Kürzel" />
        <TextBox Text="{Binding NewNoiseMeasurement.Employee}" Grid.Row="1" Grid.Column="1" />

      </Grid>

      <Label>Minmalwert in Dezibel</Label>
      <TextBox Text="{Binding NewNoiseMeasurement.MinValue}" />
      <Label>Mittelwert in Dezibel</Label>
      <TextBox></TextBox>
      <Label>Maximalwert in Dezibel</Label>
      <TextBox></TextBox>
      


      <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
        <Button Content="Überspringen" x:Name="SelectNextMeasuringPoint" IsEnabled="{Binding CanClick}" />
        <Button Content="Nächster" x:Name="AddNoiseMeasurementToSelectedMeasuringPoint" IsEnabled="{Binding CanClick}" />
      </StackPanel>
      <TextBlock Text="Alle Messpunkte ausgefuellt!" Visibility="{Binding AllDone}"></TextBlock>

    </StackPanel>
    <Grid Grid.Column="1">
      <Image Source="../Resources/Map.jpg"></Image>
      <ItemsControl ItemsSource="{Binding MeasuringPoints}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <Canvas/>
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemContainerStyle>
          <Style>
            <Setter Property="Canvas.Left" Value="{Binding Model.Position.XPosition}"/>
            <Setter Property="Canvas.Top" Value="{Binding Model.Position.YPosition}"/>
          </Style>
        </ItemsControl.ItemContainerStyle>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Ellipse Height="20" Width="20" Stroke="Black" Fill="Firebrick" StrokeThickness="{Binding Stroke}" ></Ellipse>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </Grid>
  </Grid>
</UserControl>
