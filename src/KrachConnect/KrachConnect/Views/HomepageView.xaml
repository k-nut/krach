<UserControl x:Class="KrachConnect.Views.HomepageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">


    <Grid Background="PapayaWhip" >
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="0" Grid.RowSpan="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Button x:Name="ShowMapScreen" Content="Karte" Width="50" Height="50" />
                <ContentControl x:Name="ActiveItem" />
            </Grid>
            <Grid Grid.Column="1">
                <Button x:Name="ShowBlueScreen" Content="Export" Width="50" Margin="12,0,0,0" Height="50"/>

            </Grid>

        </Grid>
        <Grid Grid.Row="0" Grid.Column="1">
            <TextBlock TextWrapping="Wrap" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"> Lärmkrach
                professional</TextBlock>

        </Grid>
        <Grid Grid.Row="1" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Button Name="MesspunktButton" Width="65" Height="50">
                    <TextBlock Text="Messpunkt hinzufügen" TextWrapping="Wrap" TextAlignment="Center" />
                </Button>
            </Grid>
            <Grid Grid.Column="1">
                <Button Name="MesswertButton" Width="65" Height="50">
                    <TextBlock Text="Messwert hinzufügen" TextWrapping="Wrap" TextAlignment="Center" />
                </Button>
            </Grid>
        </Grid>
        <Grid Grid.Row="1" Grid.Column="1">
            <TextBlock FontSize="16"  VerticalAlignment="Top"> Letzte Änderungen:</TextBlock>
            <DataGrid AutoGenerateColumns="False" 
          Height="125" 
          HorizontalAlignment="Left" 
          Name="dataGrid1" 
         VerticalAlignment="Bottom"
          Width="150">
                <DataGrid.Columns >
                    <DataGridTextColumn Header="Datum" Binding="{Binding Path=A}" MaxWidth="45" />
                    <DataGridTextColumn Header="Messpunkt"  Binding="{Binding Path=B}" MaxWidth="53" />
                    <DataGridTextColumn Header="Messwert" Binding="{Binding Path=C}" MinWidth="52" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>



    </Grid>
</UserControl>
