<UserControl x:Class="KrachConnect.Views.AlternativeEvaluationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:oxy="http://oxyplot.org/wpf"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">


  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"></RowDefinition>
      <RowDefinition ></RowDefinition>
      <RowDefinition></RowDefinition>
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" >
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="230"></ColumnDefinition>
        <ColumnDefinition Width="150"></ColumnDefinition>
        <ColumnDefinition Width="350"></ColumnDefinition>
      </Grid.ColumnDefinitions>

      <TextBlock Grid.Column="0" Text="Auswertungszeitraum: " />
      <DatePicker Grid.Column="1" SelectedDate="{Binding MinDate}" />

      <DatePicker Grid.Column="2" SelectedDate="{Binding MaxDate}" />

    </Grid>
    <Expander Header="Zeige Grenzwertüberschreitungen pro Messpunkt" Grid.Row="1" Grid.Column="0" IsEnabled="True" >
      <oxy:Plot  Model="{Binding TotalsPlotModel}">
      </oxy:Plot>
    </Expander>
    <Expander Header="Zeige alle Messpunkte" Grid.Row="2" Grid.Column="0" IsEnabled="True" >
      <oxy:Plot Model="{Binding PlotModel}">
      </oxy:Plot>
    </Expander>


  </Grid>
</UserControl>