<UserControl x:Class="KrachConnect.Views.AlternativeEvaluationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:oxy="http://oxyplot.org/wpf"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition ></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230"></ColumnDefinition>
                <ColumnDefinition Width="150"></ColumnDefinition>
                <ColumnDefinition Width="350"></ColumnDefinition>
                <ColumnDefinition Width="200"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <xctk:DateTimeUpDown Value="{Binding StartDate}"></xctk:DateTimeUpDown>
            <DatePicker Grid.Column="1" SelectedDate="{Binding EndDate}"></DatePicker>

            <TextBlock Grid.Row="0" Grid.Column="2" >Diese Messpunkte haben zuletzt den Grenzwert überschritten:</TextBlock>
            <ItemsControl Grid.Row="0" Grid.Column="3" ItemsSource="{Binding TooLoudMeasurements}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding MeasuringPoint.Name}"/>
                            <TextBlock Text=" "/>
                            <TextBlock Text="{Binding MaxValue}"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
        <Expander Header="Zeige Grenzwertüberschreitungen pro Messpunkt" Grid.Row="1" Grid.Column="0" IsEnabled="True" >
            <oxy:Plot  Model="{Binding TotalsPlotModel}">
            </oxy:Plot>
        </Expander>
        <Expander Header="Zeige alle Messpunkte" Grid.Row="2" Grid.Column="0" IsEnabled="True" >
            <oxy:Plot Model="{Binding PlotModel}">
            </oxy:Plot>
        </Expander>


    </Grid>
</UserControl>