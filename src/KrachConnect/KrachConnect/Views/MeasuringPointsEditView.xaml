<UserControl x:Class="KrachConnect.Views.MeasuringPointsEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:krachConnect="clr-namespace:KrachConnect"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition></RowDefinition>
      <RowDefinition Height="*"></RowDefinition>
    </Grid.RowDefinitions>
    
    <Grid Grid.Row="0">
      <Grid.RowDefinitions>
        <RowDefinition/>
        <RowDefinition/>
        <RowDefinition/>
        <RowDefinition/>
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition/>
        <ColumnDefinition/>
      </Grid.ColumnDefinitions>

      <TextBlock Grid.Row="0" Grid.Column="0" Text="Messpunktname" VerticalAlignment="Center"/>
      <TextBox   Grid.Row="1" Grid.Column="0" Height="30" Width="300" Text="{Binding SelectedMeasuringPoint.Name}" HorizontalAlignment="Left" VerticalAlignment="Top"/>
      <TextBlock Grid.Row="2" Grid.Column="0" Text="Bemerkung" VerticalAlignment="Center"/>
      <TextBox   Grid.Row="3" Grid.Column="0" Height="300" Width="500" Text="{Binding SelectedMeasuringPoint.Notes}" HorizontalAlignment="Left" VerticalAlignment="Top"/>
  
      <Label Content="Archiviert" Grid.Column="1" Grid.Row="0"></Label>
      <CheckBox Grid.Column="1" Grid.Row="1" IsChecked="{Binding SelectedMeasuringPoint.IsArchived}"></CheckBox>
      <Button Content="Speichern" x:Name="SaveToHub" Grid.Column="1" Grid.Row="2"/>
    </Grid>
    
    <Grid Grid.Row="1">
      <Image Cursor="Cross" Source="../Resources/map1.JPG" cal:Message.Attach="[Event MouseLeftButtonUp] = [Action AddNewMeasuringPoint($scaledmousex, $scaledmousey)]"/>
      <ItemsControl ItemsSource="{Binding MeasuringPoints}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <Canvas />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemContainerStyle>
          <Style>
            <Setter Property="Canvas.Left" Value="{Binding XPosition}"/>
            <Setter Property="Canvas.Top" Value="{Binding YPosition}"/>
          </Style>
        </ItemsControl.ItemContainerStyle>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Ellipse cal:Message.Attach="[Event MouseDown] = [Action ChangeSelectedMeasuringPoint($datacontext)]"
                     Cursor="Hand"
                     Fill="{Binding IsArchived, Converter={krachConnect:IsArchivedToBrushConverter}}"
                     StrokeThickness="{Binding IsSelected, Converter={krachConnect:BoolToStrokeThicknessConverter}}"
                     Height="20" Width="20"
                     Stroke="Black">
              <Ellipse.ToolTip>
                <StackPanel>
                  <TextBlock Text="{Binding Name}"/>
                  <TextBlock Text="Ist Archiviert" Visibility="{Binding IsArchived, Converter={krachConnect:BoolToVisibilityConverter}}"/>
                </StackPanel>
              </Ellipse.ToolTip>
            </Ellipse>

          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </Grid>
  </Grid>

</UserControl>
